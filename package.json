{
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "format": "prettier --write .",
    "lint": "turbo run lint"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "glob": "^8.0.3",
    "prettier": "^2.7.1",
    "turbo": "^1.4.3"
  },
  "engines": {
    "node": ">=16",
    "npm": ">=7.0.0"
  }
}
