import { Breadcrumbs } from "@mantine/core";
import { useMatches } from "@remix-run/react";

export const BreadcrumbsNavigator = ({}) => {
  const matches = useMatches();

  return (
    <>
      {/* <Breadcrumbs>{items}</Breadcrumbs> */}
      hehe
    </>
  );
};
